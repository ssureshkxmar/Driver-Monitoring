# Base URL for the HTTP API
# Use your machine's local/LAN IP when developing
EXPO_PUBLIC_API_BASE=http://<DEV_HOST_IP>:8000

# Base URL for WebSocket connections
EXPO_PUBLIC_WS_BASE=ws://<DEV_HOST_IP>:8000/ws

# Base URL for the website
EXPO_PUBLIC_WEBSITE_BASE=http://localhost:3000
